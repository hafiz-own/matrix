# -----------------------------------------------------
# AUTOSTART
# -----------------------------------------------------

# The 'ff' command (Fuzzy Finder)

# -----------------------------------------------------
# Fastfetch
# -----------------------------------------------------
if [[ $(tty) == *"pts"* ]]; then
    if [ ! -f $HOME/.config/matrix/settings/hide-fastfetch ]; then
        fastfetch
    fi
fi

